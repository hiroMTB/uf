#include "../../../config/all.xcconfig"

// add to project setting
HEADER_SEARCH_PATHS = $(HEADER_CINDER) $(HEADER_BOOST) $(HEADER_SNDFILE) $(HEADER_OSC)
LIBRARY_SEARCH_PATHS = $(LIB_PATH_CINDER) $(LIB_PATH_SNDFILE)
OTHER_LDFLAGS = $(LDFLAG_CINDER_D) $(LDFLAG_SNDFILE)
